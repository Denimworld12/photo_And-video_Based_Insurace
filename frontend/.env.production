# Set this to the deployed backend origin before building for production.
# The value is baked into the JS bundle at build time by Create React App,
# so a rebuild is required whenever it changes.
REACT_APP_API_URL=https://your-backend-url.herokuapp.com
